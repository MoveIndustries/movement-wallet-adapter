---
"@moveindustries/cross-chain-core": patch
"@moveindustries/wallet-adapter-react": patch
---

Fix dead `movementlabs.xyz` URLs — the domain no longer resolves. Update
explorer links to `explorer.movementnetwork.xyz`, RPC endpoints to
`{testnet,mainnet}.movementnetwork.xyz/v1` (no `fullnode.` prefix), and the
privacy-policy/ecosystem links to `www.movementnetwork.xyz`.
